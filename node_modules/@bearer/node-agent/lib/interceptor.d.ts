/// <reference types="node" />
import { URL } from 'url';
import http from 'http';
import https from 'https';
export declare const intercept: (module: typeof http | typeof https) => typeof https.request | typeof http.request;
export declare function extractRequest(isHttp: boolean, urlOrOptions: any, optionsOrCallback: any): RequestInfo;
interface RequestInfo {
    url: string;
    fullUrl: () => string;
    parsedQuery: any;
    options: {
        protocol: string;
        hostname: string;
        port: number | undefined;
        query: string;
        headers: Record<string, string>;
        path: string;
        method: string;
    };
}
export declare function isURL(anUrl: any): anUrl is URL;
export {};
