import { Stage, RemediationData } from '../stage';
import { Rule, DataCollectionRule, LogLevel } from '../new-rules/types';
import { GlobalSanitizerParams } from '../sanitizer';
export declare class Reporter {
    private activeRules;
    private globalSanitizerParams;
    private reported;
    constructor(activeRules: ActiveRules, globalSanitizerParams: GlobalSanitizerParams);
    report(stage: Stage, remediationData: RemediationData): void;
    private buildConnectionData;
    private buildRequestData;
    private buildResponseData;
    private buildBodiesData;
    private ensureNotReported;
}
/**
 * Typing
 */
interface ActiveRules {
    activeRules: Rule[];
    activeDataCollectionRules: DataCollectionRule[];
    logLevel: LogLevel;
    active: boolean;
    sanitizeKeyPattern: RegExp | undefined;
    sanitizeValuePattern: RegExp | undefined;
}
export {};
