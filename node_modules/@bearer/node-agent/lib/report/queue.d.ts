export declare class Signal {
    isSet: boolean;
    private wakeup;
    wait(): Promise<void>;
    set(): void;
}
export declare class QueueProcessor<T> {
    private stopping;
    private forceStopSignal;
    private dequeueSignal;
    private stoppedSignal;
    private queue;
    run(handler: (item: T, stopSignal: Signal) => Promise<void>): Promise<void>;
    enqueue(item: T): void;
    stop(): Promise<void>;
    private dequeue;
    private hasItems;
}
