/// <reference types="node" />
import { ClientRequest } from 'http';
import { RetryRemediation } from './new-rules/types';
export declare class Retry {
    private performRetry;
    attempt: number;
    constructor(performRetry: () => void);
    retry(remediation: RetryRemediation | undefined): boolean;
    private backoffMs;
}
export declare const exponentialBackoffMs: ({ delay, attempt }: {
    delay: number;
    attempt: number;
}) => number;
export declare class RetryListenerMover {
    private listeners;
    pushListeners(req: ClientRequest): void;
    popListeners(req: ClientRequest): void;
}
